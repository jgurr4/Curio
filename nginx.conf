server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Keep redirects relative so host/port mapping (e.g. localhost:4173) is preserved.
  absolute_redirect off;

  location / {
    try_files $uri $uri/ =404;
  }
}
